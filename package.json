{
  "name": "az-it-jobs-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Telegram bot that alerts on new Helpdesk / Cyber Security (Blue Team) IT vacancies in Azerbaijan via the BirJob aggregator API.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "once": "node src/index.js --once",
    "get-chat-id": "node src/get-chat-id.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
